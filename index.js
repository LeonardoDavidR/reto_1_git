
const suma = (x,y,z) =>{
    resultado = x+y+z;
    // console.log(resultado);
    return restultado;
};



const resta = (x,y) => {
    resultado = x-y;
    // console.log(resultado);
    return resultado;
}

const producto= (x,y) =>{
    resultado = x*y;
    // console.log(resultado);
    return resultado;
}

const divison = (x,y) => {
    resultado = x/y;
    // console.log(resultado);
    return resultado;
}

const cuadrado = (x) => {
    resultado = x*x;
    return resultado;
}

module.exports = {suma , resta , divison , producto};